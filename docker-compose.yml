#
# docker-compose.yml for Hermes Agent
#
# Usage:
#   HERMES_UID=$(id -u) HERMES_GID=$(id -g) docker compose up -d
#
# Set HERMES_UID / HERMES_GID to the host user that owns ~/.hermes so
# files created inside the container stay readable/writable on the host.
# The entrypoint remaps the internal `hermes` user to these values via
# usermod/groupmod + gosu.
#
# Security notes:
#   - The dashboard service binds to 127.0.0.1 by default. It stores API
#     keys; exposing it on LAN without auth is unsafe. If you want remote
#     access, use an SSH tunnel or put it behind a reverse proxy that
#     adds authentication — do NOT pass --insecure --host 0.0.0.0.
#   - The gateway's API server is off unless you uncomment API_SERVER_KEY
#     and API_SERVER_HOST. See docs/user-guide/api-server.md before doing
#     this on an internet-facing host.
#
services:
  gateway:
    build: .
    image: hermes-agent
    container_name: hermes
    restart: unless-stopped
    # network_mode: host works only on Linux. On macOS/Windows we publish
    # ports explicitly and bind to 127.0.0.1 so they're not reachable on
    # the LAN by accident.
    ports:
      - "127.0.0.1:8642:8642"   # OpenAI-compatible gateway API (when API_SERVER_HOST is set)
    volumes:
      - ./hermes-home:/opt/data
    environment:
      - HERMES_UID=${HERMES_UID:-10000}
      - HERMES_GID=${HERMES_GID:-10000}
      # OpenAI-compatible HTTP API enabled. Container binds 0.0.0.0
      # internally; host publish rule restricts external reach to
      # 127.0.0.1:8642. API_SERVER_KEY comes from the .env file
      # alongside this compose file (auto-loaded).
      - API_SERVER_HOST=0.0.0.0
      - API_SERVER_KEY=${API_SERVER_KEY}
      # Microsoft Teams — uncomment and fill in to enable Teams gateway.
      # Register your bot at https://dev.botframework.com/ to get these values.
      # - TEAMS_CLIENT_ID=${TEAMS_CLIENT_ID}
      # - TEAMS_CLIENT_SECRET=${TEAMS_CLIENT_SECRET}
      # - TEAMS_TENANT_ID=${TEAMS_TENANT_ID}
      # - TEAMS_ALLOWED_USERS=${TEAMS_ALLOWED_USERS}
      # - TEAMS_PORT=${TEAMS_PORT:-3978}
    command: ["gateway", "run"]

  dashboard:
    image: hermes-agent
    container_name: hermes-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:9119:9119"
    depends_on:
      - gateway
    volumes:
      - ./hermes-home:/opt/data
    environment:
      - HERMES_UID=${HERMES_UID:-10000}
      - HERMES_GID=${HERMES_GID:-10000}
    # Bind 0.0.0.0 inside the container; the `ports:` mapping above
    # restricts external reach to 127.0.0.1 on the host, so this is not
    # LAN-exposed. `--insecure` is required to acknowledge the 0.0.0.0
    # bind (dashboard stores API keys; the flag is its safety opt-in).
    command: ["dashboard", "--host", "0.0.0.0", "--no-open", "--insecure"]
